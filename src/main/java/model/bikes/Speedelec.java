package model.bikes;

public class Speedelec extends ElectricBike {
    public Speedelec(String brand, int weight, boolean isAvailableLight, String color,
                     int price, int maxSpeed, int batteryCapacity) {
        super(brand, weight, isAvailableLight, color, price, maxSpeed, batteryCapacity);
    }
}
